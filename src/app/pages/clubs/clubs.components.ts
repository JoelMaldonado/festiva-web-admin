import { Component, OnInit } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'clubs-component',
  imports: [RouterModule],
  template: `<router-outlet />`,
})
export class ClubsComponents {}
// 